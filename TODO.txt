- Potentially do multi threading to speed up script
- Loading media should create raw images first before prompting user on selection
- Frame sampling so you can pick a specific frame to run FX on.
- Opting to keep both files should just put the new folder inside the folder path instead of the root folder